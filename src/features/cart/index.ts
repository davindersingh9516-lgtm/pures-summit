/**
 * Cart feature module - cart drawer/page UI. State already exists at
 * `@/contexts/cart-context.tsx`; this folder holds the UI that consumes it
 * (line item rows, cart summary, mini-cart trigger).
 */
export {};
