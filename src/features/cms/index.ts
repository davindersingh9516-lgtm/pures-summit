/**
 * CMS feature module - renders backend-authored `ContentBlock` unions
 * (see types/cms.types.ts) for the homepage and generic pages. The block
 * renderer registry (block type -> component) lands in the next phase.
 */
export {};
